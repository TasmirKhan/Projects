# Gemini
# Add one key, or comma-separate backup keys to rotate when a key/model is quota-limited.
GEMINI_API_KEY=

# Optional email automation placeholders
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Optional future automation integrations
NOTION_API_KEY=
HUBSPOT_API_KEY=
